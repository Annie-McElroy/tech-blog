// Comment are related to Post
// Post can have many Comment
// Comment belongs to many w/ Post and User
// Post belong to User
// User can have many Post

const User = require('./User');
const Post = require('./Post');
const Comment = require('./Comment');